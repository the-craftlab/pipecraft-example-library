# pipecraft-example-library

Single-branch PipeCraft example: a library that **publishes directly from `main`** with
no promotion flow. Conventional commits drive version → tag → GitHub release on `main`.
